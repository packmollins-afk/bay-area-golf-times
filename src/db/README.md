# Database Layer - PROTECTED

## Files:
- `schema.js` - Database initialization and table creation
- `courses.js` - Course data, seeding, and queries

## DO NOT modify without:
1. Backing up data/golf.db first
2. Testing locally with `node api/index.js`
3. Verifying API endpoints still work

## Schema changes require:
1. Update schema.js
2. Delete and recreate database (or write migration)
3. Re-seed data
4. Test all API endpoints
